// One flat delivery charge, the same for every city in Pakistan and the same
// however the order is paid for.
// Both the checkout screen and the order API read it from here, so the number
// the customer is shown is always the number they are charged.
export const DELIVERY_CHARGE = 250;
